<!DOCTYPE html>
<html>
  <head>
  <title>People <%= @user.username %> follows | Tweeter</title>
  </head>
  <body>
    <nav>
      <a href="/show?id=<%= @user.id %>">Home</a>
      <a href="/followers?id=<%= @user.id %>">Followers</a>
      <a href="/following?id=<%= @user.id %>">Following</a>
      <a href="/upload">Test upload</a>
      <a href="/logout">Logout</a>
    </nav>
    <h1>Following:</h1>
    <ul>
      <% @following.each do |followed| %>
      <li><a href="/show?id=<%= followed.id %>">@<%= followed.username %></a></li>
      <% end %>
    </ul>
  </body>
</html>